sala-nera-hero.mp4 is the live hero clip. To swap it, replace that file keeping the name.

Still wanted here:
  hero-poster.jpg  - a still from the clip (~150 KB). Mobile and reduced-motion visitors
                     see this instead of the video, so without it they get a plain gradient.
                     Once added, append it to the .poster background-image in index.html.
  sala-nera-hero.webm  - optional, ~30% smaller than the mp4. If added, restore the
                     data-webm attribute on the <video> tag; browsers will prefer it.
